export const FPS = 5;

export const TALK_GAP_FRAMES = 2; // 0.4s

export const SUBTITLE_HEIGHT_PX = 320;

export const DEFAULT_SECTION_INITIAL_DELAY_FRAMES = 2; // 0.4s

export const DEFAULT_SECTION_END_FRAMES = 2; // 0.4s

export const zIndex = {
  anyValue: 1,
  subtitle: 10,
  yukkuri: 10,
  transitionMovie: 10000,
} as const;
